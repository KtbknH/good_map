# Assets images

Déposez ici le logo officiel Good Maps : `logo.png` (référencé par `AppAssets.logo`).

En attendant, le widget `GoodMapsLogo` (lib/widgets/good_maps_logo.dart)
reconstruit le logo en texte + icône, donc l'app fonctionne sans cet asset.
